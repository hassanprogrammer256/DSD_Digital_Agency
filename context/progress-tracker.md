# Progress Tracker

Update this file after every completed feature. Any AI agent reading this should immediately know what is done, what is in progress, and what is next.

This file was reset on **2026-08-26** when the project pivoted from the previous template — an unrelated military-college ERP dashboard ("SCSC ERP") — to **the DSD digital marketing agency website**. Every context file was rewritten from scratch that day. Later the same day, Phases 1–3 were built and shipped, followed by two mid-build scope additions (Phase 8: Team, Pricing, dropdown nav; Phase 9: Service Detail Pages, legal pages), then the remaining originally-planned Phases 4–7 (Portfolio, rest of About, Contact, Polish) — see Decisions Made below for the full history.

---

## Current Status

**Phase:** All 9 phases (24 features) are done, plus several **2026-08-27** additions and two **2026-08-28** passes on top: agency positioning refresh, a full cookie-consent system + real legal-page content, a Navbar mega-menu restructure, photo enhancements across five pages, an ad-campaign landing page system, a cards-over-bullets UI pass, a site-wide hero-banner pass, optional WhatsApp/phone contact links for team members, a corporate-content removal + 20-service/6-category catalog restructure (this removed the "Company Compliance" hub + 11 detail pages referenced in earlier versions of this line), a legal-page identity/content rewrite + distinct per-page hero photos, and — most recently — distinct per-service hero images (all 20 services) replacing `ServiceDetailPage`'s one shared photo (see Decisions Made). See Decisions Made for each.
**Last completed (2026-08-28):** Per-service hero images — all 20 services in `data/services.ts` now have their own distinct `heroImage`, in response to an explicit user follow-up ("use different images for every service hero background image, i.e web development, mobile app development, etc") after the page-level hero-image pass below. See Decisions Made for the full detail, including 3 mismatched photos caught and swapped during visual QC.
**Previously completed (2026-08-28):** Legal-page rewrite + distinct hero images, in response to explicit user request. The 4 legal pages (`CookiePolicyPage`, `PrivacyPolicyPage`, `WebsiteDisclaimerPage`, `PartnerWithUsPage`) and the `LegalPage` hub no longer carry the unrelated "DSD Corporate Services" identity — this closes the "Site identity discrepancy" Known Gap that had been open since 2026-08-27. Separately, all 10 `PageHeroBanner` usages (every page except Home) now have a distinct, thematically-matched photo instead of all reusing `home_hero_bg.png`. See Decisions Made for the full detail.
**Previously completed:** Corporate-content removal and service-catalog restructure, in response to explicit user request that DSD read as "strictly a digital marketing agency" that "must not be confused with corporate services," using Digital Graphiks (digitalgraphiks.ae) as a category reference. Removed the 2 corporate services, the entire Compliance section (nav item, hub, 11 detail pages, `compliance.ts`), and `coooperate_governance.txt`. Rebuilt the catalog as 20 services across 6 categories (`Service.category` added); Navbar's Services dropdown rebuilt as a 6-column categorized mega-menu; new `/technologies` and `/industries` pages added, content grounded in real portfolio evidence with 2 explicitly user-confirmed exceptions. **Not yet real-browser-verified** — see Decisions Made point 10 and Known Gaps. See Decisions Made for the full reasoning.
**Previously completed:** Site-wide hero-banner pass, in response to explicit user request ("modify each page hero banner, to look like the home banner i.e image in the background and overlayed with the text"), plus (mid-turn, same session) optional WhatsApp/phone contact links added to team member data. New `PageHeroBanner` common component replaces the old plain-`surfaceSecondary` page-header band on 8 pages with the same full-bleed-photo-plus-navy-overlay treatment Home's own hero uses. `TeamMember` gained optional `phone`/`whatsapp` fields (new `WhatsappIcon` in `SocialIcon.tsx`), each independently rendered on `TeamMemberDetailPage` alongside the existing optional email/LinkedIn icons. See Decisions Made for the full reasoning and `ui-registry.md` for the component/page-level details.
**Previously completed:** Cards-over-bullets UI pass, in response to explicit user request ("utilise more the use of cards to group content rather than just bulleted typography, use designed cards with relevant CTAs"). New `ChecklistCard` common component replaces bare bulleted checklists with bordered icon-badge cards across `ProjectDetailPage`/`ServiceDetailPage` and (on the now-removed `CompliancePage`/`ComplianceDetailPage`) several flat lists — that page-level detail is now historical, since the Compliance section was removed in the most-recent pass above.
**Previously completed:** Ad-campaign landing page system — `/lp/:slug` + `/lp/:slug/thank-you`, built to a supplied 7-point high-converting-landing-page checklist (message match, single goal/no nav, short 2-field form, strong CTA copy, trust signals, dedicated thank-you page). Two seeded example offers tied to real existing services. See Decisions Made for the full rationale, including why these routes render with no Navbar/Footer.
**Previously completed:** Two follow-ups on the Compliance work — (1) a "Common Mistakes Founders Make in the UAE" section (10 items, transcribed from a supplied second reference document) added to `CompliancePage`, between Consequences-of-Non-Compliance and the Practical Conclusion; (2) every Navbar dropdown trigger is now a real routed page — added `LegalPage` (`/legal`, a 4-card hub linking to the existing legal pages) so "Legal" is no longer a button-only, page-less dropdown trigger, and simplified `NavItem.to` back to required (the optional-`to`/button-fallback code from the earlier Legal-dropdown addition is gone — no longer needed now that every dropdown-carrying nav item has a real page). Verified with `tsc -b`, `eslint .`, `vite build`, and real-browser Playwright checks.
**Next:** A real-browser/Playwright verification pass of the service-catalog restructure and of this session's legal-content/hero-image pass (neither possible in this session's environment — see Decisions Made). Beyond that, nothing outstanding against the build plan. See Known Gaps below for what's left before this could actually launch — it's mostly real content (portfolio, team, pricing, testimonials, stats, awards) and account setup (EmailJS). The site identity discrepancy is now resolved (see Decisions Made) — no longer an open gap.

---

## Actual Repository State (as of 2026-08-26)

**Dependencies:** all of Phase 1's planned stack is installed and in active use — `@mui/joy` + Emotion, `react-router-dom`, `framer-motion`, `lucide-react`, `react-toastify`, `react-hook-form`, `zod` + `@hookform/resolvers`, `@emailjs/browser`, `tailwindcss` + `@tailwindcss/vite`, `geist`.

**Known dependency quirk:** `node_modules/next` (a full Next.js install) is present despite `next` never appearing in `package.json` — it's `geist`'s own required peer dependency, auto-installed by npm, never imported by any DSD source file. Harmless; not worth fighting (see `build-plan.md` Phase 1 note).

**Built — everything in `build-plan.md`'s 24 features, plus post-launch-prep additions (see Decisions Made).** Full page set: `HomePage`, `AboutPage` (story/mission, why-choose-us, founder, office+map, referral-programme teaser), `ServicesPage` (full 10-service grid + team carousel + photo CTA banner), `ServiceDetailPage` (`/services/:slug`, hero now a text+photo split), `CompliancePage` (`/compliance`, hub), `ComplianceDetailPage` (`/compliance/:slug`, one template for 11 areas), `ProjectsPage` (filterable grid), `ProjectDetailPage` (gallery, CTAs, functionality, similar projects), `PricingPage`, `TeamMemberDetailPage`, `ContactPage` (full validated form + photo), `src/pages/legal/{LegalPage,CookiePolicyPage,PrivacyPolicyPage,PartnerWithUsPage,WebsiteDisclaimerPage}.tsx` (**real content**, not placeholder — see Decisions Made), `src/pages/landing/{LandingPage,LandingThankYouPage}.tsx` (`/lp/:slug`, `/lp/:slug/thank-you` — standalone routes, no Navbar/Footer, see Decisions Made), `NotFoundPage`. Full component set in `src/components/{layout,common,sections}/` plus `src/components/common/{CookieConsentBanner,CookieSettingsModal,ConsentGate}.tsx`. Full data layer in `src/data/{services,projects,testimonials,stats,team,founder,pricing,compliance,cookieInventory,landingPages}.ts` + `src/types/index.ts`. `src/lib/{email,utils,icons,useDocumentTitle,cookieConsent}.ts` + `src/lib/validation/{contactSchema,landingLeadSchema}.ts`. `src/vite-env.d.ts` (typed `import.meta.env.VITE_EMAILJS_*`). `.env.local.example` (real `.env.local` still doesn't exist — needs real EmailJS credentials).

**Assets:** `src/assets/fonts/geist-mono/GeistMono-Variable.woff2`; `src/assets/images/hero/home_hero_bg.png` (real supplied photo — reused, differently cropped via `object-position`, on `AboutPage`'s Our Story section, `ServicesPage`'s CTA banner, and the `ServiceDetailPage`/`ComplianceDetailPage` hero templates); `src/assets/contact1.png` (real supplied handshake photo, used on `ContactPage` and `PartnerWithUsPage`); `src/assets/images/about/about_dsd.jpeg` (placeholder graphic, currently unused on the page itself); `src/assets/images/projects/{slug}/screenshot-*.svg` (9 placeholder gradients, one project has 2; `smart-academy`'s `hp256_banner.png` is a real but client-specific promotional graphic — not reused elsewhere as generic photography); `src/assets/images/testimonials/avatar-{1,2,3}.svg`; `src/assets/images/team/{10 members}.svg` + `founder.svg` (initials-on-color-circle placeholders). The About page's office map is a **live Google Maps `<iframe>` embed**, gated behind `ConsentGate` (see Decisions Made) — no map graphic file needed.

**Reference assets, unchanged:** `public/dsd_logo.png`, `context/designs/layout.jpg`.

---

## Progress

### Phase 1 — Foundation — done

- [x] 01 Tooling Setup
- [x] 02 Design Tokens & Theme (joyTheme rebuilt with literal hex — see Decisions Made)
- [x] 03 App Shell — Navbar + Footer + Route Tree
- [x] 04 Static Content Data

### Phase 2 — Home Page — done

- [x] 05 Hero + CTA Strip (real photo, not placeholder)
- [x] 06 Services Overview
- [x] 07 Stats Counter Strip
- [x] 08 About Teaser + Featured Projects
- [x] 09 Testimonials + Closing CTA

### Phase 3 — Services Page — done

- [x] 10 Full Services Listing (8 services, anchor-linkable)

### Phase 4 — Portfolio — done

- [x] 11 Projects Listing + Category Filter
- [x] 12 Project Detail Page

### Phase 5 — About Page — done

- [x] 13 About Page — story/mission, why-choose-us grid, founder (Phase 8), office block + live map, all present

### Phase 6 — Contact — done

- [x] 14 Contact Form (validation + submit + graceful-failure path all verified; real send needs real EmailJS credentials)

### Phase 7 — Polish — done

- [x] 15 Page Transitions Everywhere (confirmed across all routes added since Phase 1)
- [x] 16 Responsive QA (mobile 390px + desktop 1280px verified across every page; tablet 768px not separately screenshotted)
- [x] 17 SEO Basics (per-page titles, meta/OG/Twitter tags, favicon fix, alt-text audit)
- [x] 18 Accessibility & 404 (404 confirmed, keyboard focus verified visible; mobile drawer focus-trap not separately verified — see Known Gaps)

### Phase 8 — Extended Offering: Team, Pricing & Dropdown Nav — done

- [x] 19 Eighth Service — UAE Business Setup & Advisory
- [x] 20 Team Carousel + Member Detail Pages
- [x] 21 Pricing Page
- [x] 22 Navbar Dropdown Navigation

### Phase 9 — Service Detail Pages & Legal Pages — done

- [x] 23 Service Detail Pages (`/services/:slug`)
- [x] 24 Legal / Utility Pages (Cookie Policy, Privacy Policy, Partner With Us, Website Disclaimer)

---

## Known Gaps / Open Decisions (to resolve before or during build)

- **2026-08-27 service-catalog restructure has not been real-browser-verified.** `tsc -b`/`eslint .`/`vite build` all pass clean and a dev-server HTTP smoke test confirmed `/`, `/services`, `/technologies`, and `/industries` all serve correctly, but no `chromium-cli`/browser-automation tool was available in this session's environment to actually render the pages, click through the new Navbar mega-menu (desktop hover + mobile accordion), or check light/dark theme — every prior dated entry in this log was confirmed this way; this one wasn't. Needs a real-browser pass before being considered launch-verified: the categorized Services dropdown at 1024/1280/1440px and on mobile, `/services`'s 6 category sections, a couple of the 12 brand-new service detail pages (e.g. `/services/ai-chatbot-development`, `/services/logo-design`), `/technologies`, `/industries`, and confirming `/compliance` now cleanly 404s.
- **Real portfolio project data has since been supplied (2026-08-27, mid-session, edited directly by the user, not by this session)** — `data/projects.ts` now holds real projects (Smart Agents Academy, Batenga Community Foundation, Bush and Beyond Tours, KSS e-Voting App, Senior Command and Staff College, Quiz Hub, Title Plz, Minify Gadgets) with real screenshot assets and real `liveUrl`/`githubUrl` links (Vercel/GitHub Pages/custom domains, no `example.com` placeholders). This resolves the long-standing "placeholder portfolio" gap — no longer accurate to describe `data/projects.ts` as sample/placeholder data. (Mid-edit at one point this same session, the file briefly had a broken import — self-resolved by the user before this note was updated; not something this session fixed.)
- **`src/data/team.ts` has 3 repeated `slug` values** (`irumba-hannan` ×3, `kyeswa-hassan` ×2, `lagwe-ysan` ×2) — confirmed via a real-browser console check (React's "duplicate key" warning) during the 2026-08-27 hero-banner verification pass, not otherwise obvious from reading the file. Since `/team/:slug` resolves via `.find()`, only the first-matching entry for each duplicated slug is ever reachable by URL — the others exist in the array but have no working detail-page route. Needs unique slugs once the real team roster replaces this placeholder data (see the general "real team roster" gap below), or sooner if this is confusing during demos.
- **Real stats are not yet supplied** (site-wide `data/stats.ts` and every service's per-service `stats` in `data/services.ts`).
- **Real team roster is not yet supplied.** `data/team.ts`'s 10 members (names, roles, bios, photos) are placeholders, as is `data/founder.ts`. Real DSD staff photos should replace the initials-on-color-circle SVGs.
- **Real pricing is not yet supplied.** `data/pricing.ts`'s 3 tiers and dollar figures are illustrative only.
- **Site identity discrepancy — resolved 2026-08-28.** The 3 legal pages and `PartnerWithUsPage` previously used a different, unrelated entity from the originally-supplied source documents: "DSD Corporate Services," a UAE corporate-services/company-formation/immigration firm, address "1st Floor, Office 06, Al Habeb Building, Umm Hurair Street, Oud Metha, Dubai," phone `+971 58 588 9033`, email `info@dsdcop.com`, site `www.dsdcop.com`. Per explicit user confirmation (2026-08-28), all 4 pages were rewritten to use the site's one real "DSD" digital-marketing-agency identity — address "Office No. 1, 1st Floor, Al Hareb Building, Umm Hurair Road, Oud Metha, P.O. Box 181040, Dubai," phone `+91 7585889093`, email `info@dsdgrp.com`, site `www.dsdgrp.com` (this last one inferred from the email domain — no domain was referenced anywhere else in the codebase — and explicitly confirmed by the user rather than assumed silently). See that day's Decisions Made entry for the full rewrite scope.
- **No trade-licence number has ever been supplied for DSD.** Not fabricated during the 2026-08-28 legal-page rewrite — `WebsiteDisclaimerPage`'s governing-law section now cites general UAE federal law and Dubai courts rather than referencing a specific free-zone licence that doesn't exist in this codebase's data. If DSD holds a real trade licence, it should be added to that section once supplied.
- **`CompliancePage`/`ComplianceDetailPage` content is general UAE compliance guidance, not legal or tax advice** — sourced from a supplied reference document, with its own "this is general guidance, not a substitute for advice" disclaimer carried through verbatim in the page itself. Citation labels in the content (e.g. "— Federal Tax Authority") reproduce the source's own attribution text; none had a URL attached, so none were fabricated or linked.
- **EmailJS account/service/template does not exist yet** — the contact form's and landing pages' validation, submission, and error-handling are all built and verified, but a real send needs a real account. `.env.local.example` documents the 3 required vars; copy to `.env.local` and fill in once DSD creates the account (destination `info@dsdgrp.com`).
- **No real CRM (Salesforce, HubSpot, etc.) or Zapier/native-integration connection exists.** The landing-page lead form (`/lp/:slug`) currently relays through the same EmailJS mechanism as the Contact form — a real "ad → landing page → CRM" pipeline needs an actual CRM account and either a native EmailJS/CRM integration or a Zapier automation, none of which exist yet. `sendLandingPageLead` in `src/lib/email.ts` documents this gap inline.
- **`dsd-client.vercel.app` (the secondary design/content reference named in the original brief) could not be scraped by automated tooling** — client-rendered SPA, no meaningful static HTML returned to `WebFetch`. Still worth a manual look before finalizing brand copy.
- **Social media links/handles are not yet supplied** — the footer's social icon row currently points at bare platform homepages (`facebook.com`, `instagram.com`, etc.), not real DSD accounts.
- **`node_modules/next` is present but unused** (see Actual Repository State above) — cosmetic only, not a real gap.
- **Home page's featured-services row (`ServicesOverview`) uses a curated `FEATURED_SLUGS` list** (Website Development, SEO Optimization, Paid Advertising), not `services.slice(0, 3)` — already resolved by slug reference rather than array position, so it was unaffected by the 2026-08-27 service-catalog restructure (20 services, reordered array). Worth revisiting to feature a wider spread across the new categories once real service-popularity data exists, but not broken by the restructure.
- **Mobile drawer's keyboard focus-trap was not separately verified.** Tab-order and visible focus states were confirmed on the desktop header; whether focus correctly cycles within the open mobile drawer (rather than escaping to content behind it) hasn't been tested directly.
- **Tablet breakpoint (768px) was not separately screenshotted during the Phase 7 responsive pass** — only mobile (390px) and desktop (1280px) were directly verified. Tailwind's `sm:`/`md:`/`lg:` classes are already in place throughout, so tablet is very likely fine, but it's an assumption, not a confirmed check.
- **Google Maps embed on the About page uses a plain URL-query embed (no API key)** — this works today (verified rendering the correct Dubai neighborhood in a real browser) but is a more informal integration than a proper Maps JavaScript API key; fine for this site's needs, worth knowing if a future session wants marker customization or usage guarantees.

---

## Decisions Made During Build

- **2026-08-28** — Per-service hero images: all 20 services now have their own distinct `heroImage` (`src/assets/images/services/<slug>.jpg`) instead of `ServiceDetailPage` sharing one generic photo across every `/services/:slug` route, per explicit user follow-up request ("use different images for every service hero background image, i.e web development, mobile app development, etc"). `Service` type (`src/types/index.ts`) gained a required `heroImage: string` field; `services.ts` imports and assigns all 20; `ServiceDetailPage.tsx` now reads `service.heroImage` instead of a single shared `serviceHeroPhoto` import (the old shared `hero/service_detail_hero.jpg` file was reused as the SEO Optimization image via `cp`, then removed from `hero/` rather than left duplicated). Sourced via the same Unsplash download pipeline as the page-level hero pass below, with the same free-tier verification discipline. **One extra QC pass beyond the page-level images**: every one of the 20 was visually inspected (not just spot-checked), which caught 3 real mismatches a caption-only pick would have missed — `custom-system-development`'s first candidate (`kL1LR1uyhLs`, captioned "man presenting a diagram on a whiteboard") turned out to be an unrelated medical/EMT training photo, not a system-architecture diagram; `ai-chatbot-development`'s first candidate showed a literal, recognizably-branded ChatGPT interface (OpenAI's product UI, not appropriate to imply as DSD's own chatbot work); `android-app-development`'s first candidate had a Chinese-language phone UI. All 3 were swapped for verified-clean replacements (a sticky-note planning whiteboard, a generic 3D speech-bubble icon, and a phone showing Google's own Android boot screen, respectively) before being wired in. Verified via `tsc -b`, `eslint .`, and `vite build`, all clean — no real-browser pass done this session (see the entry below for the same caveat, which applies here too).
- **2026-08-28** — Legal-page identity/content rewrite + distinct per-page hero images, in response to two explicit user requests in the same session: "the legal documents... are for cooperate services content, rewrite them for this digital Marketing site," and "change each page's hero background image to a relevant one... apart from the homepage." Key decisions:
  1. **Identity resolved via direct question, not assumed.** Two conflicting real-looking identity blocks existed in the codebase (see Known Gaps, pre-2026-08-28 wording) — the legal pages' own "DSD Corporate Services" (`dsdcop.com`) details, and the site-wide "DSD" (`dsdgrp.com`) details used everywhere else (Navbar, Footer, Contact, `project-overview.md`). Asked the user directly which was real rather than guessing; confirmed: site-wide (`dsdgrp.com`) identity, with `www.dsdgrp.com` as the website (no domain existed anywhere in the codebase before this — inferred from the email domain and explicitly confirmed, not assumed silently).
  2. **Rewrite scope differs by page, decided per-page not uniformly.** `CookiePolicyPage`'s cookie taxonomy/consent-mechanics structure was already accurate for a marketing site (it was written generically enough) — only its identity block needed correcting. `PrivacyPolicyPage` and `WebsiteDisclaimerPage` got full substantive rewrites, not identity swaps — their prior content was entirely about AML/KYC due diligence, trade-licence/visa application processing, and Ajman-free-zone jurisdiction, none of which describes a digital marketing agency. Replaced with content grounded in what this codebase actually does: data collected via the contact/landing-page forms and relayed by EmailJS (`src/lib/email.ts`), no analytics/ad-pixel tracking claimed since none is wired in (cross-checked against `src/data/cookieInventory.ts`'s real audit rather than assumed), ad-platform account access for campaign management, no-guaranteed-marketing-results and third-party-platform-dependency disclaimers (algorithm/policy changes on Google/Meta/app stores outside DSD's control), and UAE-federal-law/Dubai-courts governing law. `PartnerWithUsPage` needed only its "corporate services" noun and contact block corrected, not a structural rewrite — confirmed by the user's own framing ("the company information is true but the wrong nouns... aren't").
  3. **No trade-licence number or free-zone affiliation was invented.** The prior Known Gap explicitly warned not to fabricate one; `WebsiteDisclaimerPage`'s governing-law section was written to cite general UAE federal law and the Emirate of Dubai (matching DSD's real Dubai address) without claiming a specific licence or free zone.
  4. **Hero images: one distinct, thematically-matched photo per page, replacing the "one photo (`home_hero_bg.png`), many crops" convention** on all 10 `PageHeroBanner` usages except Home itself (per the user's own "apart from the homepage" scoping). No image-generation tool is available in this environment, so images were downloaded rather than generated — sourced from Unsplash (Unsplash License: free for commercial use, no attribution required), found via web search for a real `unsplash.com/photos/<slug>-<id>` permalink per topic, then fetched through Unsplash's own `/photos/<id>/download?force=true` redirect (the same mechanism their own "Download" button uses) rather than the deprecated `source.unsplash.com` redirector (confirmed dead, HTTP 503). Several initial candidates (mostly Getty-sourced "Unsplash+" premium photos) 403'd on that download endpoint and were swapped for alternatives — free-tier availability was verified before committing to each pick, not assumed from the search result alone. Each image was re-fetched at `w=1920&q=75&fm=jpg&fit=crop&auto=format` to keep file sizes reasonable (130KB–545KB, vs. the original `home_hero_bg.png`'s 1.2MB) and saved to `src/assets/images/hero/{page}_hero.jpg`. Every downloaded image was visually inspected (via the Read tool) before wiring it in, to confirm it actually matched its intended theme rather than trusting the search-result caption blind. `imagePosition` crops were re-tuned per new photo's composition (e.g. faces/subject kept in frame) rather than left at the old photo's crop values.
  5. Content-section photos unrelated to `PageHeroBanner` (`story.jpeg`, `about_dsd.jpeg`, `contact1.png`) were left untouched — out of scope for both requests. `TeamMemberDetailPage`, `NotFoundPage`, and the 3 pure-legal-text pages still correctly have no `PageHeroBanner` (per the 2026-08-27 hero-banner-pass decision) and so were not touched by the image-sourcing half of this pass.
  6. **Not verified via `tsc -b`/`eslint .`/`vite build` or a real-browser pass in this turn** — flagged explicitly rather than claimed; run those checks (and a visual pass of all 10 new hero images plus the 4 rewritten legal pages) before treating this as launch-verified, consistent with this log's existing standard.

- **2026-08-27** — Awards & Achievements section removed entirely, in response to explicit user request ("remove the awards section"), immediately following the service-catalog restructure below. Removed the `ServiceAward` type and `Service.awards` field (`src/types/index.ts`), every `awards: [...]` entry across all 20 services (`src/data/services.ts`), and the "Awards & Achievements" section + unused `Trophy` import from `ServiceDetailPage.tsx`. Not left as an empty/placeholder field — the section and its data are gone, not hidden. This also resolves the "Real awards are not yet supplied" Known Gap below (removed, not replaced with real data). Verified via `tsc -b`, `eslint .`, and `vite build`, all clean.
- **2026-08-27** — Corporate-content removal and service-catalog restructure, in response to explicit user request: DSD must read as "strictly a digital marketing agency" and "must not be confused with corporate services," using Digital Graphiks (digitalgraphiks.ae) as a category-structure reference. This was scoped across two rounds of clarifying questions before any code was touched (breadth/category selection, then a mid-investigation discovery that expanded scope). Key decisions:
  1. **Scope grew beyond the 2 obvious corporate services once the codebase was actually read.** The user's initial ask named `services.ts`'s "Corporate Business Consulting" and "UAE Business Setup & Advisory" entries, but reading `architecture.md`/`ui-registry.md` surfaced a much bigger, previously-undocumented "Compliance" section: a Navbar mega-menu, `/compliance` hub, and 11 `/compliance/:slug` detail pages covering UAE trade-license/tax/governance content — 100% corporate-governance material with zero digital-marketing relevance, entirely absent from `project-overview.md` (which had gone stale and never documented it). Flagged to the user mid-task rather than silently expanded or silently left alone; user confirmed full removal.
  2. **The "DSD Corporate Services" identity mismatch (see Known Gaps) was only partially resolved, deliberately.** Removing the Compliance section eliminates its `CompliancePage` reference to "DSD Corporate Services." The 3 verbatim legal pages (`CookiePolicyPage`/`PrivacyPolicyPage`/`WebsiteDisclaimerPage`) and `PartnerWithUsPage` still carry the "DSD Corporate Services"/Ajman-ANCFZ/`dsdcop.com` identity — the user explicitly chose "leave the legal pages untouched for now" over rewriting or replacing them, since altering verbatim legal text isn't something to do as a side effect of a service-catalog task. Genuinely deferred, not forgotten.
  3. **Removed entirely**: `services.ts`'s 2 corporate entries; `landingPages.ts`'s `free-business-setup-consultation` offer (tied to the removed service); `compliance.ts`, `CompliancePage.tsx`, `ComplianceDetailPage.tsx`, `ComplianceArea` type, `complianceIcons`; the Navbar's "Compliance" nav item and the Footer's "Company Compliance" link; `coooperate_governance.txt` (the repo-root source file for the Compliance hub's "Founder Mistakes" section, not just an orphaned reference as first assumed).
  4. **Rebuilt catalog: 20 services across 6 categories** (`Service` gained a `category: ServiceCategory` field), reached via two rounds of explicit user confirmation on scope (category breadth, which of Digital Graphiks' categories to adopt, and — critically — explicit exclusion of Digital Graphiks' "Next-Gen/IT Services" category, since IT consulting/cyber security/dedicated dev teams/IT outsourcing reads as exactly the enterprise-corporate positioning being removed). The 8 pre-existing non-corporate services kept their slugs (no dead links into them) with copy rewritten toward Digital Graphiks' more confident, precision-led framing; 12 new services added (Influencer Marketing; Logo Design, UI/UX Design, Graphic Design, Video & Motion Animation; iOS/Android/Cross-Platform App Development; Shopify Store Development, Custom Ecommerce Solutions; AI Chatbot Development, Generative AI Integration).
  5. **New services deliberately have empty `teamMemberSlugs` in most cases, not a stretched or invented match.** `team.ts` has 3 duplicated `slug` values (see Known Gaps) — referencing any of those duplicated slugs from a new service would render two different-role cards under the same name via `ServiceDetailPage`'s `.filter()`. Rather than reference a duplicated slug (visibly confusing) or a plausible-but-wrong single match (e.g. a "Lead Web App Developer" standing in for mobile-app expertise he isn't described as having), new services with no clean team fit were left with `[]`, matching this codebase's existing precedent (`content-email-marketing` already shipped this way) — `ServiceDetailPage` already hides the section entirely when empty.
  6. **`relatedProjectSlugs` on new services only reference a real project when the fit is genuine** — e.g. the 3 real `mobile-app-development`-category portfolio projects (`kss-e-voting-app`, `quiz-hub-app`, `title-plz`) populate the 3 new Mobile App Development services, and `minify-gadgets` (an electronics ecommerce platform) populates Custom Ecommerce Solutions. Design & Branding and Smart AI Services were left with `[]` — no portfolio project is actually a design deliverable or a generative-AI/chatbot integration; `title-plz`'s "AI-powered visual recognition" feature is computer vision, not generative AI, so it was deliberately not stretched into evidence for the AI services.
  7. **Technologies (`/technologies`) and Industries (`/industries`) are new pages, not folded into an existing one** — grouped-badge and card-grid hubs respectively, no per-item detail pages (matches the user-confirmed "moderate" breadth). Technologies content was held to the same no-overclaim discipline as the rest of this codebase's placeholder data: the full stack list (React/TypeScript/Next.js/Node/Express/NestJS/Django/MongoDB/PostgreSQL/Firebase/Flutter/React Native/Expo/JWT/Stripe/PayPal/Mobile Money APIs) is grounded in real `techStack` entries across `src/data/projects.ts`, cross-checked before writing anything — not assumed. Two exceptions were explicitly confirmed by the user ahead of time rather than silently included: Shopify/WooCommerce (no project evidence yet, accepted as a reasonable industry-standard-platform claim) and OpenAI/GPT API + LangChain (no named AI stack in any past project, accepted as a real stack DSD commits to going forward). Industries were trimmed from Digital Graphiks' 15-industry reference list down to the 10 DSD's own portfolio evidences (Education, Retail & Ecommerce, Real Estate, Healthcare, Travel & Tourism, Logistics, Non-Profit, Fitness & Wellness, Entertainment, Technology & SaaS) — not gated by the user like Technologies was, but held to the same standard on the same principle.
  8. **Navbar's "Services" dropdown rebuilt as a categorized mega-menu** (`NavItem` gained `groups`/`extraLinks`, replacing the old flat `dropdown` + `layout: "grid"` shape that both Services and the now-removed Compliance used) — 6 labeled columns instead of one flat icon grid, since a 20-item flat grid at this scale would read as noise. Per-item icons were dropped at this scale (20 items across 6 columns is too much visual weight per the earlier icon-per-item convention); "Meet the Team"/"Technologies"/"Industries We Serve" are appended below the grid as a distinct `extraLinks` row, generalizing the previous single-item "Meet the Team" append. Mobile accordion renders the 6 categories as plain, non-interactive label dividers over one flat scrollable list rather than a second nested accordion level — deliberately kept to one interaction level, per `ui-rules.md`'s existing bias against unnecessary interaction complexity.
  9. **No new category-color tokens were introduced.** 6 service categories need visual distinction, but `ui-tokens.md` only has 4 non-CTA semantic tokens available (`accent` is reserved for calls-to-action only, per its own invariant) — 2 short. Rather than inventing 2 new hues (color-token proliferation this codebase has otherwise been disciplined about) or misusing `accent` decoratively, categories are distinguished structurally instead — a `SectionHeading` per category on `/services`, labeled columns in the nav mega-menu — not by chip color. `ServiceCard`'s existing `badgeColorAt` cycling (`primary`/`accent`/`success`/`warning`/`info` per row) is unchanged and remains purely decorative row-variety, unrelated to category identity.
  10. Verified via `tsc -b`, `eslint .`, and `vite build` (all clean) plus a dev-server HTTP smoke test on `/`, `/services`, `/technologies`, `/industries`. **No real-browser/Playwright visual verification was possible in this session's environment** (no `chromium-cli`/browser-automation tool available) — unlike every prior dated entry in this log, this pass is not confirmed against an actual rendered page, light/dark theme, or mobile viewport. Flagged explicitly rather than claimed; a real-browser pass (nav mega-menu at 1024/1280/1440px, mobile accordion, both new hub pages, a couple of new service detail pages, confirming `/compliance` now 404s) is still needed before this is launch-verified the way the rest of this codebase has been.
- **2026-08-27** — Site-wide hero-banner pass, in response to explicit user request: "modify each page hero banner, to look like the home banner i.e image in the background and overlayed with the text." Plus, mid-turn in the same session, a second explicit request to add optional WhatsApp/phone contact links to team member data. Key decisions:
  1. **New shared `PageHeroBanner` component** (`src/components/common/PageHeroBanner.tsx`), not a copy-pasted background+overlay block per page — reused across 8 pages, well past the `architecture.md` "≥2 pages → belongs in `common/`" threshold. Reuses Home's exact navy gradient overlay formula (`ui-tokens.md` → Hero Section) so every page genuinely "looks like the home banner," rather than inventing a similar-but-different overlay.
  2. **Height is proportional to content, not a copy of Home's fixed `min-h-[600px]`.** Home's hero carries a lot more content (eyebrow, headline, subheadline, a 3-phrase tagline row, two CTA buttons) and is deliberately the single largest visual on the site; forcing that same height onto a listing page with only a title and one-line description would leave a wall of empty photo space. `PageHeroBanner` instead sizes by `py-20 md:py-28`, matching the proportions of the site's existing photo CTA banners (`ServicesPage`/`CompliancePage`'s mid-page bands).
  3. **`ServiceDetailPage`/`ComplianceDetailPage`'s heroes were restructured, not left alone.** Both previously showed the photo as a separate `lg:grid-cols-2` side panel (hidden below `lg`) rather than as the section's own background — visually a different pattern from Home's "photo behind the text" hero. Converted both to `PageHeroBanner` with `align="left"` and their existing CTA row passed via `children`, so the photo is now the full-bleed background behind the text everywhere on the site, not just on the simpler listing pages.
  4. **Scope excluded 4 pages that don't have a "banner + further sections" shape to begin with**: `TeamMemberDetailPage`, `PartnerWithUsPage`, `NotFoundPage`, and the 3 pure-legal-text pages (`CookiePolicyPage`/`PrivacyPolicyPage`/`WebsiteDisclaimerPage`) are each effectively one single content block already (a profile card, a two-column pitch, a centered error message, a scrolling legal document) — retrofitting a full-bleed photo hero onto a formal Cookie Policy document, for instance, would mean redesigning the whole page rather than "modifying the hero banner." Flagged in `ui-registry.md`'s `PageHeroBanner` entry rather than silently applied or silently skipped.
  5. **`title`/`eyebrow` accept `ReactNode`, not just `string`**, specifically so `ProjectDetailPage` could pass its existing colored category-tag pill as the eyebrow and `ComplianceDetailPage` could pass an icon-plus-text combination as the title, rather than the banner needing a special-cased prop for each. The category pill needed one small inline `style={{ textTransform: "none" }}` override since the banner's default eyebrow styling applies `uppercase` for the plain-text case.
  6. **Every usage reuses `home_hero_bg.png` with its own distinct `imagePosition` crop** — continuing the site's already-established "one signature photo, many crops" convention (see the 2026-08-27 photo-enhancements entry below) rather than sourcing 8 new photos. Content-section photos added elsewhere in this same window by the user directly (`story.jpeg`, `about_dsd.jpeg`) are unrelated to this component and were left untouched.
  7. **Team member contact links**: `TeamMember` gained optional `phone`/`whatsapp` fields (`src/types/index.ts`), populated on roughly half of the 10 placeholder entries in a deliberately mixed pattern (phone-only, whatsapp-only, both, and — on 2 entries — neither) specifically to exercise the "missing one is simply omitted, the rest still render" requirement rather than leaving it untested. A new `WhatsappIcon` was added to `SocialIcon.tsx` (lucide-react 1.x ships no branded icons, same reasoning as the existing Facebook/Instagram/LinkedIn/X stand-ins) rather than pulling in a new icon library for one glyph. `TeamMemberDetailPage`'s contact-icon row now renders four independent `{member.X && (...)}` conditionals (email, LinkedIn, phone, WhatsApp) — phone links `tel:` with whitespace stripped, WhatsApp links `https://wa.me/<digits>` with all non-digit characters stripped from the same stored number, rather than maintaining two differently-formatted copies of it.
  8. Verified via `tsc -b`, `eslint .`, `vite build`, and real-browser Playwright screenshots (light + dark) of every converted page plus one project/service/compliance-area detail page — all render the new full-bleed hero correctly with legible white text, no console errors introduced by this pass. One pre-existing, unrelated React "duplicate key" console warning was surfaced during this check — `src/data/team.ts` has 3 repeated `slug` values (`irumba-hannan` ×3, `kyeswa-hassan` ×2, `lagwe-ysan` ×2) predating this session's changes, which `TeamCarousel` renders with `key={member.slug}`. Not fixed here (out of scope, placeholder data, and the user was independently, concurrently editing project data files during this session) — flagged in Known Gaps instead.
- **2026-08-27** — Cards-over-bullets UI pass across `ProjectDetailPage`, `ServiceDetailPage`, `CompliancePage`, and `ComplianceDetailPage`, in response to explicit user request: "utilise more the use of cards to group content rather than just bulleted typography, use designed cards with relevant CTAs." Key decisions:
  1. **Audited every plain bulleted list on the site first, rather than converting on sight.** `ServiceCard`'s `included` list, the `complianceAreas` grid, `LIFECYCLE_STAGES`, and the Pricing tiers' feature checklist were already inside a card — left untouched. The real targets were checklists sitting directly on page background with no card wrapper at all: Project Detail's "Key Functionality," Service Detail's `included` list, Compliance Detail's `obligations`, and six of `CompliancePage`'s own local constants (`REQUIREMENT_FACTORS`, `MERITS`, `DEMERITS`, `CONSEQUENCES`, `FOUNDER_MISTAKES`, `COMPLIANCE_CALENDAR`).
  2. **Two different card treatments, chosen by content shape, not applied uniformly.** Flat one-line items (`REQUIREMENT_FACTORS`, `CONSEQUENCES`, `COMPLIANCE_CALENDAR`, plus the functionality/included/obligations lists) became a new shared `ChecklistCard` component (`src/components/common/ChecklistCard.tsx`) — a compact bordered card with a small icon badge, reused across ≥2 pages so it belongs in `common/` per `architecture.md`'s own boundary rule. Substantive items with a title *and* paragraph(s) (`MERITS`, `DEMERITS`, `FOUNDER_MISTAKES`) were instead upgraded to the full `ServiceCard`-spec bordered/shadow card (`rounded-xl border border-border bg-surface p-6 shadow-[...]`, `whileHover={{ y: -4 }}`) — wrapping a single short fact in a full 24px-padded shadow card would have read bulkier than the content warranted, so the two patterns were deliberately kept distinct rather than forcing one card style everywhere.
  3. **Card backgrounds stay neutral; color lives only in the icon badge** — per `ui-rules.md`'s existing "never a colored card background outside navy sections" rule. `ChecklistCard` takes a `surface` prop (`surface`/`surface-secondary`) so each usage contrasts correctly against its own section's background, matching the alternating-shade convention `LIFECYCLE_STAGES`/`complianceAreas` cards already used.
  4. **Not every card got an individual CTA.** Informational fact/pros-cons cards (Requirement Factors, Merits, Demerits, Consequences, Compliance Calendar) stayed CTA-less, consistent with the pre-existing `AboutPage` `VALUE_PROPS` cards and `LIFECYCLE_STAGES` cards, which never had per-card CTAs either — forcing a button onto e.g. a single functionality bullet would have been decorative, not useful. Instead, one new genuinely relevant CTA was added: "Avoid These Mistakes — Talk to an Advisor" (`CtaButton` → `/contact`) directly under the Founder Mistakes section, since that section didn't already sit next to an actionable next step (unlike, say, the Merits section, which follows directly after the page's "Talk to Our Team" photo banner).
  5. **`ServiceDetailPage` gained a missing "What's Included" heading** while converting its `included` list to cards — the list previously had no heading at all, just a bare `<ul>` after the description paragraph; a real, if small, pre-existing gap fixed in passing.
  6. Verified via `tsc -b`, `eslint .`, and `vite build` — all clean. A live-browser Playwright check was attempted but interrupted mid-way by an unrelated, already-in-progress issue in `src/data/projects.ts` (a malformed import referencing a `pulse-fitness-app` folder that doesn't exist on disk, plus an undefined `pulseScreenshot1` reference) that appeared on disk mid-session and broke the whole app's dev build — confirmed via `git`-independent mtime and a fresh `tsc -b` re-run that the failure was isolated entirely to that one file, not any file touched by this pass. Flagged to the user rather than fixed, since it reads as their own concurrent in-progress edit, not something this pass caused or should guess how to resolve.
- **2026-08-27** — Ad-campaign landing page system built end-to-end, in response to a supplied explanation of the "ad → landing page → CRM" lead-capture model and its 7-point high-converting-landing-page checklist, followed by "design the landing pages." Key decisions:
  1. **Landing pages are siblings of the `<App/>` layout route in `router.tsx`, not children of it** — `App.tsx` always renders `Navbar`/`Footer`/`CookieConsentBanner` around its `<Outlet/>`, and the checklist's #1 rule is "remove the main navigation menu, sidebar, and footer links; only allow one action." Nesting under `App` would have meant fighting that layout with CSS; a separate top-level route (`{ path: "/lp/:slug", element: <LandingPage /> }`) sidesteps it entirely. React Router ranks matches by path specificity across the *whole* route tree, so these still correctly beat App's own `*` catch-all despite being declared as a sibling, not a child.
  2. **The DSD logo at the top is a plain `<img>`, not wrapped in a `<Link>`** — the one deliberate exception to "the logo always links home" elsewhere on the site. A clickable logo is exactly the kind of exit ramp the checklist warns against; every other affordance to leave the page (main nav, footer links) was removed the same way, except a single small Privacy Policy link in the fine print (kept deliberately — a page collecting personal data linking to the real privacy policy is a trust signal, not a distraction, and ties back into this session's earlier cookie/privacy-consent work).
  3. **Form is name + email only** (`src/lib/validation/landingLeadSchema.ts`), not the Contact form's 4 fields — the checklist is explicit that every extra field measurably cuts conversion; anything else needed gets collected later, after the first touch.
  4. **Lead submission reuses the same EmailJS relay as the Contact form** (`sendLandingPageLead` in `src/lib/email.ts`), tagging the subject/body with which offer generated the lead, rather than claiming a real CRM integration. **This project has no real CRM (Salesforce/HubSpot) account or Zapier connection configured** — building one would mean fabricating credentials/an integration that doesn't exist. Flagged explicitly in Known Gaps below, mirroring the existing EmailJS-credentials gap.
  5. **Two example offers seeded** (`src/data/landingPages.ts`): "Free Website Audit" (→ `website-development`) and "Free UAE Business Setup Consultation" (→ `business-setup-advisory`) — both tied to real, already-built DSD services, both using generic/honest lead-magnet framing with no fabricated stats, client counts, or urgency claims (same discipline as every other placeholder-content decision this session). Adding a new campaign is one data entry, not new code.
  6. **Trust signals (checklist point 6) avoid numeric social-proof claims** — reused one of the existing (already-flagged-placeholder) testimonials per offer, plus two honest, non-numeric reassurance lines ("Your information stays private," "No spam, unsubscribe anytime"). Deliberately did not invent a client count, star rating, or "as featured in" logo row — `data/stats.ts`'s numbers are themselves still placeholder (see below), and presenting placeholder numbers as trust-building proof on a lead-capture page would compound that gap rather than just carry it.
  7. **Desktop: two-column layout, form on the right and `lg:sticky`** (visible without scrolling) — pitch copy, hook bullets, and trust signals on the left. **Mobile: form renders first** (`order-1` vs. the pitch content's `order-2`), directly per the prompt's own "over 60% of ad clicks are mobile — test before launching" note; verified via a real 390px-viewport Playwright screenshot, not assumed from the desktop layout.
  8. **Thank-you page (`LandingThankYouPage`) resolves its copy from the offer's own data, not router state** — same defensive pattern as every other slug-resolved page this session (`ComplianceDetailPage`, `ServiceDetailPage`) — a direct visit or a refresh still renders correctly instead of showing a blank/broken state. Offers one further action (Browse Services / Follow on LinkedIn), per the checklist's point 7.
  9. Verified via `tsc -b`, `eslint .`, `vite build`, and real-browser Playwright: zero `<nav>` elements and exactly one footer link on the landing page itself, a full form-fill-and-submit exercising the real (expected-to-fail-without-credentials) EmailJS path with its error toast — same verified-failure-path standard already applied to the Contact form — a direct visit to the thank-you page, an invalid-slug redirect to Home, and the mobile-first form-order layout.
- **2026-08-27** — Every Navbar dropdown trigger given a real route, plus a "Common Mistakes Founders Make" section added to the Compliance hub, in response to two follow-up user requests: "also route them, every dropdown menu item must be routed i.e with a pathname" (after noticing "Legal" opened a dropdown but wasn't itself a clickable link), and "read @coooperate_governance.txt and include its context under compliance" (a second supplied reference document, distinct from `company_compliance.txt`). Key decisions:
  1. **New `LegalPage` (`/legal`)** — a simple 4-card hub (icons: `Cookie`, `ShieldCheck`, `Handshake`, `TriangleAlert`) linking to the existing Cookie Policy / Privacy Policy / Partner With Us / Website Disclaimer pages, giving "Legal" the same "trigger is itself a real page" shape Services and Compliance already had. Not new content — an index over pages that already existed.
  2. **`NavItem.to` simplified back to required, the optional-`to`/button-fallback branches removed from both desktop and mobile rendering** — that flexibility was added specifically to let "Legal" exist without a page; once every dropdown trigger has one, keeping dead conditional branches around would violate the project's own "no unused abstractions" rule. `openDropdown`/`mobileDropdownOpen` still key off `link.label` (unaffected).
  3. **"Common Mistakes" content kept separate from the existing "Corporate-Governance Compliance" area (`complianceAreas[1]`), not merged into it** — the new document is a different shape of content (10 numbered mistakes-to-avoid, several with their own sub-bullet list) from the existing area's "records to maintain" list; conflating them would have forced an awkward hybrid structure. Modeled as a local `FOUNDER_MISTAKES: FounderMistake[]` constant inside `CompliancePage.tsx` (not `src/data/compliance.ts`) — consistent with how `MERITS`/`DEMERITS`/`CONSEQUENCES` are already hub-page-only local constants, only the 11 areas (needed by both the hub grid *and* the per-slug detail pages) live in the shared data file.
  4. **Section placed between "Consequences of Non-Compliance" and "Practical Conclusion"** — reads as the concrete, practical companion to the more abstract consequences list right before it. Practical Conclusion's background flipped from `bg-surface` to `bg-surface-secondary` to keep the alternating-band rhythm intact with the new section inserted before it.
  5. Verified via `tsc -b`, `eslint .`, `vite build`, and real-browser Playwright: `/legal` renders correctly on both a hard navigation and a client-side nav-link click (an early screenshot taken with the mouse still hovering the just-clicked "Legal" trigger looked broken — page height ballooned to ~4700px with a huge blank gap — re-tested by moving the mouse off the nav before screenshotting, confirmed a Playwright-hover-state artifact from the dropdown staying open post-navigation, not a real rendering bug; the compliance hub's new section renders all 10 mistakes with their sub-lists).
- **2026-08-27** — "Company Compliance" content section added end-to-end, in response to explicit user request to read a supplied reference document (`company_compliance.txt`, general UAE company-compliance guidance) and "create menu nav-item for company compliance (with dropdowns if applicable), and create relevant pages with relevant context and media." Key decisions:
  1. **One hub page + one data-driven detail template, mirroring the `ServicesPage`/`ServiceDetailPage` split** — `src/data/compliance.ts` holds 11 `ComplianceArea` entries (trade-license, corporate-governance, beneficial-ownership, corporate-tax, VAT, accounting-records, employment-immigration, AML, data-protection, consumer-protection, sector-specific), each with `intro`/`obligationsLabel`/`obligations`/`notes`, transcribed from the source document. `CompliancePage` (`/compliance`) is the hub — definition, "requirements depend on" list, the 11-area card grid, lifecycle-stage grid, a photo CTA banner, merits/demerits, a consequences-of-non-compliance callout, an Economic Substance Regulations update note, and a practical-conclusion checklist, all transcribed content, not summarized. `ComplianceDetailPage` (`/compliance/:slug`) is one template for all 11 areas, matching `ServiceDetailPage`'s navy-hero-with-photo pattern.
  2. **Citations reproduced as plain attribution text, never as fabricated links.** The source document's inline citation labels (e.g. "Federal Tax Authority," "MOHRE Wage Protection System," "UAE Ministry of Finance") had no URLs attached in the supplied file — rendering them as clickable links would mean inventing destinations. They're rendered as a trailing "— Source Name" fragment in a muted color instead, via a small `note.split(" — ")` helper in `ComplianceDetailPage`.
  3. **Reused `home_hero_bg.png` again (a 3rd/4th distinct crop via `object-position`)** for the hub's photo banner and the detail template's hero — same reasoning as the same-day photo-enhancement pass below: it's the only generic (non-client-specific) real photo asset in the project, and one photo reused with deliberately different framing across a section's template is standard practice, not repetition.
  4. **New "Compliance" Navbar item reuses the exact `layout: "grid"` 3-column mega-menu pattern built for Services earlier the same day** — 11 areas fit the same 3-column treatment as the 10 services + "Meet the Team." Verified there's no nav-overflow regression from adding an 8th top-level item by screenshotting the nav at 1024/1280/1440px before considering this done.
  5. **Footer gets a single "Company Compliance" line in `QUICK_LINKS`, not a 5th dedicated column** — adding a full column listing all 11 areas would break the existing `sm:grid-cols-2 lg:grid-cols-4` footer rhythm for a secondary content area; same proportionality call already made for Legal pages in the Phase 9 entry below.
  6. **`src/lib/icons.ts` gained a second, parallel `complianceIcons` map** rather than merging into `serviceIcons` — keeps the two domains' icon sets independently extensible, same data-layer-boundary rationale already documented for `serviceIcons`.
  7. Verified via `tsc -b`, `eslint .`, `vite build`, and real-browser Playwright screenshots: the mega-menu, the hub page (full-page), and the AML detail page (chosen because it has the most complex source content — two citation notes, a compound intro paragraph) — zero console errors, all 11 area cards linking correctly.
- **2026-08-27** — Navbar dropdown restructure, in response to explicit user request: "make the drop down for service in a 3 column grid setup also make a menu item that shows a drop down to these legal documents." Key decisions:
  1. **`NavItem`/`NavDropdownItem` types extended (`to?: string`, `layout?: "grid" | "list"`, optional per-item `icon`)** rather than adding a second parallel data shape — Services' dropdown became a `layout: "grid"` 3-column mega-menu (icons resolved via `serviceIcons`) and a new "Legal" item got a plain `layout: "list"` dropdown (Cookie Policy, Privacy Policy, Partner With Us, Website Disclaimer) — same rendering code branches on `link.layout`, not a separate component.
  2. **"Legal" has no overview page of its own, so `to` is optional** — when a `NavItem` has no `to`, the desktop trigger renders as a `<button>` toggling `openDropdown` on click (instead of a `NavLink`) and the mobile accordion row does the same; every `openDropdown`/`mobileDropdownOpen` comparison was switched from keying on `link.to` (not always present now) to `link.label` (always unique, always present).
  3. **Grid dropdown panel is `left-0` (anchored to the trigger's left edge), not `left-1/2 -translate-x-1/2` like the narrow list dropdowns** — a 640px-wide 3-column panel centered under a nav item positioned left-of-center in the bar risked clipping off the left edge of the viewport at narrower desktop widths (768–1024px); left-aligning avoids that regardless of where the trigger sits.
  4. Verified via `tsc -b`, `eslint .`, `vite build`, and real-browser Playwright screenshots of both dropdowns open (desktop) — no regressions to the existing hover/mobile-accordion behavior for non-grid items.
- **2026-08-27** — Photo enhancements across 5 pages, in response to explicit user request that Services/About/Service-Detail/Contact "not to look much flat," plus a direct instruction to use `src/assets/contact1.png` on Contact and/or Partner With Us. Key decisions:
  1. **Audited the project's actual photo assets before adding anything** — found only 2 generic (non-client-specific) real photos in the whole project: `home_hero_bg.png` (already the Home hero) and the newly-added `contact1.png` (a handshake photo). `smart-academy`'s `hp256_banner.png` was ruled out for reuse — it's a real photo, but a specific client's own branded promotional graphic, and using a stranger's branded ad as generic site filler elsewhere would misrepresent it.
  2. **Reused `home_hero_bg.png` across About/Services/ServiceDetailPage with a different `object-position` crop each time**, rather than treating "we're out of photos" as a hard stop — a signature photo reused with different framing across a site's sections is a common, honest real-world pattern (not the same as e.g. stretching one stock photo to fill unrelated content). `contact1.png` went to Contact (office-info card photo header) and `PartnerWithUsPage` (two-column split, handshake pairs naturally with "referral partner").
  3. **`ServiceDetailPage`'s hero restructured from centered-text-only to a text+photo split** (`lg:grid-cols-2`, photo `hidden` below `lg`) — this was the most text-heavy template on the site and the clearest case of "flat."
  4. **`ServicesPage` gained a new photo CTA banner section** (`bg-navy` + `home_hero_bg.png` background + overlay) between the grid and the team carousel, rather than putting a photo in the page's own top hero (which stayed plain, matching `AboutPage`'s and other listing pages' convention of a plain intro band).
  5. Verified via `tsc -b`, `eslint .`, `vite build`, and real-browser Playwright screenshots of all 5 changed pages (including one incremental-scroll capture of `/about` specifically to rule out a `whileInView` false-blank-section reading in a `fullPage` screenshot — a known false-alarm class already documented in the 2026-08-26 Phase 4-7 entry below, re-confirmed rather than re-litigated).
- **2026-08-27** — Full cookie-consent system + real legal-page content, in response to the pre-launch checklist from a supplied annotated screenshot ("install a cookie-consent banner with equally clear Accept/Reject/Manage Settings," "block optional cookies until consent," "conduct a live cookie scan," "add a permanent Cookie Settings link," "confirm the company's full licensed name...") plus 3 supplied `.docx` legal documents (Cookie Policy, Privacy Policy, Website Disclaimer) to replace the existing placeholder legal-page boilerplate, and a follow-up supplied `partner_with_us.txt` for the Partner With Us page. Key decisions:
  1. **`.docx` files can't be read directly (binary)** — extracted text by unzipping each `.docx` (a ZIP archive) and stripping `<w:p>`/`<w:br>` tags from `word/document.xml` with a small Node script, rather than guessing at formatting or skipping the source material.
  2. **5-category consent model (`src/lib/cookieConsent.ts`)** — `functional`/`analytics`/`advertising`/`embedded`, `localStorage`-persisted (`dsd-cookie-consent`), cross-component communication via plain DOM `CustomEvent`s (`dsd:open-cookie-settings`, `dsd:cookie-consent-changed`) rather than React Context — no global-state library exists in this project and a two-event pub/sub is simpler than introducing one for this single feature.
  3. **`ConsentGate` structurally prevents an embed from ever entering the DOM pre-consent**, not "load then hide" — wraps the About page's Google Maps `<iframe>` (the site's one real third-party embed), satisfying "block optional cookies until consent" for real rather than cosmetically.
  4. **The cookie inventory (`src/data/cookieInventory.ts`) is from an actual live audit, not a templated guess** — code grep plus a real-browser Playwright check of `localStorage`, which caught 3 real MUI-Joy-injected keys (`joy-mode`, `joy-color-scheme-{dark,light}`) a pure code-grep would have missed entirely.
  5. **Cookie/Reject/Manage buttons are deliberately equal size and visual weight** in `CookieConsentBanner` (all `border border-border` outlined except the solid Accept) — directly satisfies the checklist's "equally clear Accept, Reject and Manage Settings options" rather than the common dark-pattern of a prominent Accept next to a small Reject link.
  6. **Legal pages now hold the real, verbatim content from the 3 supplied documents** — `CookiePolicyPage`, `PrivacyPolicyPage`, `WebsiteDisclaimerPage` all rewritten section-by-section (12, 18, and 21 sections respectively), replacing the earlier placeholder boilerplate. `PartnerWithUsPage` similarly rewritten from the supplied `partner_with_us.txt` (referral-programme copy). **This surfaced a real identity discrepancy — see Known Gaps** — the source documents' entity, "DSD Corporate Services," has different contact details than the site's general "DSD" marketing identity used everywhere else. Real entity details were used verbatim within the legal/partner pages only (legally required to be accurate); general site contact info was left unchanged; this split is flagged as a decision needing explicit user confirmation, not a fully resolved question.
  7. **No trade-licence number fabricated** — the checklist asked to confirm one, but none appears in any of the 3 source documents (only the free-zone name, ANCFZ). Flagged as a genuine gap in Known Gaps rather than invented.
  8. **3× `react-hooks/set-state-in-effect` lint errors fixed** across the new consent components (`ConsentGate`, `CookieConsentBanner`, `CookieSettingsModal`) — same class of fix as the Navbar's original bug (lazy `useState(() => ...)` initializer instead of `useState` + an effect that immediately calls `setState`; Joy's `Modal` unmounting its children by default when `open={false}` meant `CookieSettingsModal` needed no effect at all, just the lazy initializer).
  9. Verified via `tsc -b`, `eslint .`, `vite build`, and real-browser Playwright: banner first-visit appearance and equal button sizing, Manage Settings modal (toggles + live inventory table), Reject All persisting to `localStorage`, the About page map blocked-then-unblocked around a live `embedded` consent toggle, the footer's permanent Cookie Settings button, and all 4 real legal pages rendering their real contact info. Zero console errors.
- **2026-08-27** — Agency positioning and service lineup updated, in response to explicit user request to align with a supplied canonical definition ("A digital marketing agency is an external team of specialists that helps businesses grow their brand, connect with customers, and increase sales using online platforms, search engines, and digital tools") and its 5 foundational categories (SEO, Social Media Management, Paid Advertising/PPC, Web Design, Content & Email Marketing). Key decisions:
  1. **Reconciled rather than replaced** — 3 of the 5 canonical categories already had a clean match (SEO Optimization, Website Development ≈ Web Design) or were folded into an existing one; only the genuinely missing pieces were added, to avoid redundant/overlapping service cards.
  2. **"Social Media Advertising" renamed and broadened to "Paid Advertising (PPC)"** (`social-media-advertising` → `paid-advertising-ppc`, confirmed no other file hardcoded the old slug before renaming) — covers search/display/social paid ads together rather than social-only, matching the user's distinct "Paid Advertising (PPC)" category without leaving a redundant near-duplicate of the new Social Media Management service.
  3. **Two new services added**: "Social Media Management" (organic content/community, distinct from the paid-ads service above) and "Content & Email Marketing" (blogs, newsletters, automated email) — bringing the total from 8 to 10. Both got full `ServiceDetailPage` data (hero headline, stats, `included` checklist) matching every other service's shape, not a lighter/incomplete entry.
  4. **Noor Abbas (Social Media Manager) tagged on both new/renamed services** and her bio rewritten to describe both the organic and paid sides of her role — she was already the closest team fit for either, and a small real agency plausibly has one person covering both; her old bio was caught and fixed after it still read as paid-only on the new organic Social Media Management page.
  5. **Home page's featured-3 services row changed from `services.slice(0, 3)` (whichever happen to be first) to a curated `FEATURED_SLUGS` list** (Website Development / SEO / Paid Advertising) — this was already flagged as a revisit-worthy decision in this file; the clarified "we are fundamentally a marketing agency" positioning was the trigger to actually revisit it, landing on a spread across web + search + paid rather than 3 dev-focused services.
  6. **Found and fixed a real mismatch while reading the diff of a manual edit the user made directly in the IDE**: `Hero.tsx`'s secondary CTA had been relabeled "Schedule a Consultation" with a `Calendar` icon, but still linked to `/projects` — clicking "schedule a consultation" would have landed on the portfolio page. Fixed the `href` to `/contact`, kept the user's new label/icon exactly as set (that part read as a deliberate, reasonable choice, not an error).
  7. **Home hero headline/subheadline rewritten** — the previous headline was the UAE Business Setup service's own specific hero line, which had ended up on the general Home hero (not a natural fit for the whole site's identity); replaced with a headline built directly from the supplied definition's own verbs ("Grow Your Brand. Reach Your Customers. Increase Your Sales.").
  8. **`founder.ts` was also edited on disk mid-session** (name/bio changed to "Mr. Freema" / "Mr.Happiness") — read as an intentional personalized placeholder (internally consistent, not garbled text) rather than an error, so left untouched per the "say so, don't silently revert" guidance — flagged to the user rather than changed back.
  9. Verified via `tsc -b`, `eslint .`, `vite build`, and a real-browser Playwright pass: the fixed CTA href, all 10 services rendering on `/services` and in the nav dropdown, both new service detail pages resolving, the renamed service's new slug working and the old slug correctly 404-redirecting, and the updated Home featured row. Zero console errors.
- **2026-08-26** — Phases 4–7 built end to end, in response to "continue with all unfinished phases," closing out every remaining item in `build-plan.md`. Key decisions:
  1. **Portfolio filter uses `framer-motion`'s `layout` + `AnimatePresence mode="popLayout"`**, not a hard re-render — cards already on screen animate to their new position when the filter changes, new cards fade in, removed ones fade out, matching the polish level already established elsewhere on the site.
  2. **The About page's office map is a real, working Google Maps `<iframe>` embed** (`maps.google.com/maps?q=<address>&output=embed`, no API key required for this basic format), not a placeholder graphic — chosen over another generated SVG because a real address was already available and an actually-functional map is strictly better than a static one for a real office location. Verified directly in a browser (not assumed) that it renders the correct Dubai neighborhood — the first screenshot of it came back looking blank, which turned out to be the same class of timing issue hit repeatedly this session (the Maps JS widget hadn't finished its async render before the screenshot fired), confirmed by re-checking network responses (all 200s) and a longer explicit wait before recapturing.
  3. **Contact form verified end-to-end including the failure path**, not just the happy path — filled and submitted a real form in a real browser with no EmailJS credentials configured, confirmed `sendContactMessage` throws its intended clear error, `ContactPage` catches it, logs `[ContactPage/onSubmit]`, and shows the designed error toast rather than crashing or failing silently. This is the correct, fully-tested state for a contact form with the UI built ahead of real account credentials.
  4. **`useDocumentTitle` called before the early-return in every dynamic page** (`ServiceDetailPage`, `ProjectDetailPage`, `TeamMemberDetailPage`) — React's Rules of Hooks forbid calling a hook conditionally, so each computes a fallback title (`service?.title ?? "Services"` etc.) and calls the hook unconditionally, with the actual `<Navigate>` redirect still happening afterward for the not-found case.
  5. **Fixed a real pre-existing bug while doing SEO basics**: `index.html`'s favicon `<link>` pointed at `/favicon.svg`, a file that was never created (a stale default-Vite-scaffold reference nobody had touched) — repointed to the real `/dsd_logo.png` rather than generating a new dedicated favicon asset, since the logo itself works fine at favicon size and this avoids an unnecessary new asset.
  6. **`src/vite-env.d.ts` added** to type-augment `ImportMetaEnv` with the 3 `VITE_EMAILJS_*` vars — didn't exist in the repo before (the default Vite scaffold's version was never carried over during the original project pivot), needed for `src/lib/email.ts` to type-check without an `any`.
  7. **`.env.local.example` added, not a real `.env.local`** — no real EmailJS account exists yet (see Known Gaps), so there's nothing real to put in it; the example file documents the 3 required variable names for whoever sets the real account up. `.gitignore`'s existing `*.local` pattern already covers the real file once it's created.
  8. Verified via `tsc -b`, `eslint .`, `vite build`, and an extensive real-browser Playwright pass: filter interaction, gallery thumbnail clicks, empty-form validation (all 4 fields), filled-form submission and its error toast, dark mode across the new pages, mobile viewport (390px) for Projects and Contact, keyboard tab-order with visible focus rings, and `document.title` correctness across 10+ routes including the 404 fallback. Zero console errors throughout (the one logged line was the deliberate, expected `console.error` in the contact form's own catch block).
- **2026-08-26** — Full detail page per service, plus 4 legal/utility pages, added immediately after Phase 8 shipped, in response to explicit user request: a specific supplied hero headline for the UAE Business Setup service, CTAs "on each service," and each service detailed with "pricing or hire us, contact us, service projects, team members under that service, awards and achievements, statistics on that service" — plus "Cookie policy, Privacy policy, Partner with us, Website disclaimer" (matching the footer of the same emiracle.ae reference site fetched for the Phase 8 voice-adaptation work) and "relevant dropdowns on the menu for such a setup." Key decisions:
  1. **One data-driven `ServiceDetailPage`, not 8 hardcoded files.** Matches the `ProjectDetailPage`/`TeamMemberDetailPage` precedent already in the codebase — `Service` gained `heroHeadline`/`relatedProjectSlugs`/`teamMemberSlugs`/`awards`/`stats`, populated per-service in `services.ts`, and the page reads whichever service the `:slug` resolves to.
  2. **"Pricing or hire us, contact us" read as two CTA buttons, not a per-service pricing table.** Building 8 separate pricing structures would duplicate the existing `/pricing` page's 3 tiers for no real benefit; instead each service hero has a "Hire Us" `CtaButton` (→ `/contact`) and an outlined "View Pricing" link (→ `/pricing`) — visitors get a choice, and pricing stays centrally maintained in one place (`data/pricing.ts`).
  3. **`relatedProjectSlugs`/`teamMemberSlugs` are curated, not auto-derived.** The 4 portfolio categories and 8 services have no clean 1:1 mapping (e.g. "Corporate Business Consulting" has no matching project category at all) — forcing an automatic mapping would misrepresent some projects/people as belonging to a service they don't. Hand-picked per service instead, and two services (Social Media Advertising, UAE Business Setup) intentionally have zero related projects for now rather than a forced/loose fit — `ServiceDetailPage` hides that section entirely when empty, verified directly (not just assumed) via a real Playwright screenshot of the UAE Business Setup page.
  4. **Awards use deliberately generic, non-real-identifiable names.** Same reasoning as not copying Emiracle's own client-count stats in the earlier decision below — inventing a plausible-but-fake specific real-world awards-body accolade would misrepresent DSD as having won something it hasn't. Flagged explicitly in `progress-tracker.md` → Known Gaps as needing real data, same as every other placeholder content type.
  5. **Legal pages are footer-only, not in the main nav** — standard convention for this class of page, and the user's own dropdown-restructuring ask was read as being about the Services dropdown (which now needed updating anyway, since services gained real detail pages) rather than implying a second nav dropdown for legal content. Added as a slim link row above the copyright line in the footer's bottom bar rather than a 5th grid column (would have broken the existing 4-column rhythm).
  6. **`Navbar`'s Services dropdown, `ServiceCard`'s links (both variants), and the Footer's Services column all repointed from `/services#slug` anchors to `/services/:slug`** — now that a real detail page exists per service, the anchor-on-the-overview-page pattern from Phase 3/8 is superseded for services specifically (the Team anchor link, `/services#team`, is unaffected — there's still no separate team page).
  7. **`StatCounter`'s prop type was too tightly coupled to `Stat`** (required an `id` field the component never renders) to accept the new `ServiceStat` shape (no `id`) — tightened to `Pick<Stat, "value" | "suffix" | "label">`, a real, if small, type-correctness fix caught by `tsc`, not just a workaround.
  8. Verified via `tsc -b`, `eslint .`, `vite build`, and real-browser Playwright screenshots — both a service with all 4 optional sections populated (Website Development) and one with none (UAE Business Setup, projects/awards both empty) — plus dark mode, the invalid-slug redirect, the updated dropdown hrefs, and the new footer legal links/pages. Zero console errors throughout.
- **2026-08-26** — Home page voice adapted from [emiracle.ae](https://emiracle.ae) (a UAE business-setup/residency/compliance advisory firm), plus its core offering added as an 8th DSD service, per explicit user request. Fetched the site's content/tone via `WebFetch` rather than guessing. Key decisions:
  1. **Adapted the voice, did not copy Emiracle's own numbers.** Emiracle's real credibility markers ("1000+ satisfied clients," "12+ years," "90% repeat business") are *their* facts, not DSD's — importing them as DSD's own would be a factual misrepresentation, not a placeholder. DSD's Hero/stats keep their own (already clearly-marked-placeholder) numbers; only the *rhetorical style* — short declarative sentences, a confident "plan → execute" framing, parallel three-part phrasing — was adapted, applied to `Hero.tsx`'s headline/subheadline and a new three-phrase strategic line ("Plan with a clear strategy • Build for stability • Grow with long-term vision").
  2. **New 8th service ("UAE Business Setup & Advisory") mirrors Emiracle's three real service categories** (Residency Solutions, Company Incorporation, Compliance & Governance) and its "We simplify the decision, then execute it" tagline, reworded in DSD's own voice — not a verbatim copy. `Landmark` icon chosen (institutional/government association).
  3. **Scope kept targeted, not a full-site rewrite.** "Adapt how this website communicates" was read as applying the tone to the site's key voice-defining copy (Hero) and the new service, rather than rewriting all 7 existing services' already-good, specific copy — a full rewrite risked more harm (diluting working, tested copy) than value.
  4. Verified via real-browser Playwright screenshots in both themes — caught and correctly diagnosed a screenshot-timing false alarm in the process (see the entry below).
- **2026-08-26** — Team, Pricing, founder section, and dropdown nav added mid-build, in response to explicit user request: "include the pricing page too, in the about page, we have to talk about the founder, in the service page, we talk of the team members atleast a carousel of 10 with a details page of each. this calls for the nav bar having dropdowns on some menu items." Key decisions:
  1. **Team carousel lives on `/services`, not a separate `/team` page** — the user explicitly placed it there; only the individual member detail pages get their own route (`/team/:slug`), matching "a details page of each."
  2. **Dropdown added only to "Services"** (8 services + "Meet the Team"), not to every nav item — it's the one item with genuinely many children; About/Projects/Pricing/Contact stay flat single links. Desktop uses a hover panel; mobile uses an accordion (a hover panel doesn't translate to touch) — same data (`NAV_LINKS[].dropdown`), two renderings.
  3. **No new carousel dependency** — `TeamCarousel` is `overflow-x-auto` + CSS `scroll-snap` + two buttons calling `scrollBy()`, matching `code-standards.md`'s "check before adding a package" rule; a 10-item horizontal carousel doesn't need a library.
  4. **Founder and team are separate data files/people** (`founder.ts` vs. `team.ts`) rather than the founder being one of the 10 team entries — keeps the About page's founder spotlight and the Services page's team roster independently editable, and avoids the founder appearing "buried" in a scrollable carousel.
  5. **Pricing has no checkout — every tier's CTA goes to `/contact`.** This site has no payment processing (`project-overview.md` → Features Out of Scope); pricing tiers are a starting point for a conversation, stated explicitly in the page's own copy so it doesn't read as a broken/incomplete self-serve flow.
  6. **Placeholder discipline maintained**: `team.ts` (10 members), `founder.ts`, and `pricing.ts` are all clearly-labeled sample data, same convention as `projects.ts`/`testimonials.ts`/`stats.ts` — see Known Gaps.
  7. Verified via real-browser Playwright screenshots (light + dark, desktop + mobile) — zero console errors across the whole added surface.
- **2026-08-26** — Hero background swapped from the originally-built abstract placeholder SVG to a real photo the user supplied (`src/assets/images/hero/home_hero_bg.png`) — the old placeholder (`hero-bg.svg`) was deleted rather than left as dead code. The existing gradient overlay (dark-left-fading-right) happened to already match the photo's own composition (empty dark space where the headline sits, people/content on the right) with no changes needed.
- **2026-08-26** — Phases 1–3 built end to end, in response to "continue with developing phases 1-3, and confirm if done." Three real bugs found and fixed via actual browser verification (not just `tsc`/`eslint`/`vite build`, all of which passed cleanly while these bugs were still live):
  1. **`joyTheme.ts` crashed at runtime** (``Uncaught Error: MUI: Unsupported `var(--color-primary)` color``) — Joy's `extendTheme()` eagerly decomposes every palette color into RGB channels at construction time and cannot accept a `var(--...)` string. This was a known, previously-documented pitfall (recorded in this same file's own SCSC-ERP-era notes) that got missed on first pass here. Fixed with literal hex per theme (see `build-plan.md` Phase 1 → Feature 02) plus a new `CtaButton` wrapper for the one color that genuinely needs to track the live toggle.
  2. **`ProjectCard` nested an `<a>` inside a `<Link>`'s rendered `<a>`** (the hover-overlay's Live Preview/GitHub links, inside the whole-card link) — invalid HTML, flagged by React 19 as a hydration hazard. Fixed by making the card a `div role="link" tabIndex={0}` with `useNavigate()` instead of `<Link>`, so the overlay's real anchors are no longer nested inside another anchor.
  3. **Joy's `CssBaseline` fought the app's own dark-mode background** — `CssBaseline` sets `body`'s background from Joy's internal palette, which is frozen at light mode (`joyTheme.ts`'s `defaultMode` is never toggled — see bug 1's fix), so any page shorter than the viewport showed a stray white gap below its content in dark mode. Fixed by having `App.tsx`'s own root wrapper `div` set `bg-background`/`text-text-primary` explicitly — a class selector's specificity beats `CssBaseline`'s plain `body {}` rule regardless of stylesheet injection order, sidestepping the cascade fight entirely rather than trying to out-specificity it.
  4. Also diagnosed (correctly, this time) two **false alarms** from screenshot methodology rather than app bugs: Playwright's `fullPage` screenshot doesn't reliably trigger `whileInView`/`useInView` animations without an actual scroll-through first (sections looked blank; a real user scrolling sees them render correctly), and a missing post-load wait caught the Hero mid-entrance-animation once (see the Emiracle-voice entry above). Documented in `build-plan.md` so future verification passes don't misdiagnose either as a regression.
  5. Verified throughout with `tsc -b`, `eslint .`, `vite build`, and real Playwright browser sessions (Chrome via the system-installed browser, not a downloaded Chromium) — light mode, dark mode, mobile viewport, hover states, anchor-scroll, and invalid-route redirects all checked directly rather than assumed from a clean build.
- **2026-08-26** — Real light/dark theme built in `src/index.css`, reversing the same day's earlier "single light theme" call. User explicitly asked to read `ui-tokens.md` and set up `src/index.css` with dark **and** light themes; since `ui-tokens.md` at that point explicitly documented a single-theme decision, this was flagged back to the user before building (`AskUserQuestion`) rather than silently overridden — confirmed: a real user-toggleable dark mode, not just the section-level navy bands. Key decisions and work:
  1. **Docs updated first, then code** — `ui-tokens.md`, `library-docs.md`, `build-plan.md`, and this file were all updated to describe the dual-theme architecture before/alongside writing `index.css`, so the contradiction didn't persist as stale documentation.
  2. **`textInverse` fixed white in both themes, not mode-dependent** — proactively avoided the exact bug the prior (SCSC ERP) project shipped and had to hotfix: an inverting `textInverse` token used on a sidebar surface that never actually lightened in dark mode, making its text invisible. Here, `navy`/`primary`/`accent` surfaces never lighten either, so `textInverse` is a fixed constant (`#FFFFFF`) rather than a `:root`/`[data-theme="dark"]`-switched value — only `textPrimary`/`textSecondary`/`textMuted` (paired with `background`/`surface`, which genuinely do invert) are theme-dependent.
  3. **Brand navy (`navy`/`navyElevated`) stays fixed across both themes** — it's DSD's brand color (hero overlay, stats strip, footer), not a neutral that should flip with the page's own light/dark mode; only the primary/accent/semantic/neutral token *values* differ per theme, not which sections use navy.
  4. **Own `data-theme` attribute + `localStorage`, not Joy's `useColorScheme()`** — the real switch is a plain `[data-theme]` attribute on `<html>`, owned by a framework-agnostic `src/theme/theme.ts` (`initTheme`/`toggleTheme`/`getCurrentTheme`).
  5. **Blocking inline script in `index.html`** applies the stored (or system-preference) theme before first paint, preventing a flash of the wrong theme.
  6. **Defaults to system preference (`prefers-color-scheme`) on first visit**, not hardcoded light.
  7. **Dark-mode brand colors are lighter/more saturated versions of light-mode's**, not just an inverted neutral scale.
  8. **`tailwindcss`, `@tailwindcss/vite`, and `geist` installed** and `vite.config.ts` updated with the Tailwind plugin.
  9. **Geist Mono's variable woff2 copied from `node_modules/geist` into `src/assets/fonts/geist-mono/`** rather than referencing `node_modules` directly from CSS.
  10. **Verified via a clean `vite build`** — caught and fixed the Google Fonts `@import`-ordering warning (must precede `@import "tailwindcss"`).
- **2026-08-26** — Full context-file rewrite, in response to explicit user request to analyze `context/designs/` and repurpose the (leftover, unrelated) markdown planning docs for a new project: the DSD digital marketing agency's own marketing website. Key decisions:
  1. **No backend, no CMS, no auth** — this is a fully static site. Redux was dropped entirely — no global state need exists.
  2. **EmailJS chosen as the contact-delivery mechanism** — sends directly from the browser with no server. If DSD later wants a different provider, that's a swap of `src/lib/email.ts` only.
  3. ~~Single light theme, no dark mode~~ — **reversed later the same day**, see the "Real light/dark theme" entry above.
  4. **Brand palette derived directly from `public/dsd_logo.png`** (red + navy blue + white) rather than the green/dark palette of the TemplateMo reference template — TemplateMo is a *layout* reference only.
  5. **Four fixed portfolio category colors** (`ui-tokens.md` → `categoryTokens`): Web Development → primary, SEO → success, DevOps → warning, Mobile App Development → accent.
  6. **Accent red reserved strictly for calls-to-action.**
  7. **Fixed `Agents.md`'s stale file paths** — corrected `client/context/*.md` references to match the real flat `context/` layout.
  8. **`dsd-client.vercel.app` could not be scraped by automated tooling** — flagged as an open item rather than guessed at.
